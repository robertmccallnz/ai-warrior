{
  "name": "ai-warrior-tutor",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies AI requests for The Kiwi Dialectic via AI Gateway, with subscriber quota + Perplexity referral on quota-exhaustion.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create QUOTA",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}
